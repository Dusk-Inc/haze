from enum import StrEnum

class GlobalTypes(StrEnum):
    AUDITOR = "auditor"
    REGISTRY = "registry"
    CONFIG = "config"
    CORE = "core"
    NETWORK = "network"
    NEURON_IO = "neuron_io"